#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@angular-builders/custom-webpack/node_modules/esbuild-wasm/bin/esbuild" "$@"
  ret=$?
else 
  node  "$basedir/../@angular-builders/custom-webpack/node_modules/esbuild-wasm/bin/esbuild" "$@"
  ret=$?
fi
exit $ret
